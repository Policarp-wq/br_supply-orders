import { Box, Typography } from '@mui/material'

export default function LoginPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Вход</Typography>
      <Typography color="text.secondary">
        Форма входа появится на следующем этапе.
      </Typography>
    </Box>
  )
}
